# 🆓 Бесплатная база данных для Vercel - Настройка

## ❌ Почему локальная SQLite не работает на Vercel?

Vercel - это **serverless платформа** без постоянного файлового хранилища. Файлы базы данных SQLite **не сохраняются** между вызовами функций, так как каждый запрос запускается в новой изолированной среде.

## ✅ Решение: Бесплатные облачные базы данных

### Вариант 1: Neon (Рекомендуется) ⭐

**Преимущества:**
- ✅ Бесплатно навсегда (без ограничения по времени)
- ✅ 0.5 GB хранилища
- ✅ До 3 проектов
- ✅ Автоматическая интеграция с Vercel
- ✅ PostgreSQL (совместимо с вашим проектом)

**Шаги:**

1. **Зарегистрируйтесь на Neon:**
   - Перейдите на https://neon.tech
   - Зарегистрируйтесь через GitHub

2. **Создайте базу данных:**
   - Нажмите "Create Project"
   - Выберите регион (ближайший к вам)
   - Создайте проект

3. **Получите строку подключения:**
   - После создания проекта, нажмите на название проекта
   - Найдите раздел "Connection Details"
   - Скопируйте строку `DATABASE_URL`
   - Она выглядит так:
     ```
     postgresql://username:password@host/database?sslmode=require
     ```

4. **Добавьте в Vercel:**
   - Перейдите в Vercel Dashboard → Ваш проект
   - Settings → Environment Variables
   - Добавьте новую переменную:
     - **Key**: `DATABASE_URL`
     - **Value**: вставьте строку подключения из Neon
     - **Environment**: Production, Preview, Development (отметьте все)
   - Нажмите "Save"

5. **Примените миграции:**
   ```bash
   # Через Vercel CLI (установите: npm i -g vercel)
   vercel env pull .env.local
   npx prisma migrate deploy
   npx prisma generate
   
   # Или добавьте в package.json скрипт для автоматического деплоя
   ```

6. **Инициализируйте базу данных:**
   ```bash
   npm run init-db
   npm run init-network-pairs
   ```

---

### Вариант 2: Supabase

**Преимущества:**
- ✅ Бесплатно навсегда
- ✅ 500 MB хранилища
- ✅ До 2 проектов
- ✅ Веб-интерфейс для управления данными
- ✅ PostgreSQL

**Шаги:**

1. Зарегистрируйтесь на https://supabase.com
2. Создайте новый проект
3. Получите `DATABASE_URL` из Settings → Database → Connection String
4. Добавьте в Vercel (как в варианте 1, шаг 4)

---

### Вариант 3: Railway

**Преимущества:**
- ✅ $5 бесплатных кредитов каждый месяц
- ✅ Можно продлевать бесплатно
- ✅ PostgreSQL

**Шаги:**

1. Зарегистрируйтесь на https://railway.app
2. Создайте новый проект → Add PostgreSQL
3. Получите `DATABASE_URL` из Variables
4. Добавьте в Vercel

---

## 🔧 Автоматическое применение миграций при деплое

Добавьте в `vercel.json`:

```json
{
  "buildCommand": "npm run build",
  "installCommand": "npm install && npx prisma generate",
  "postinstall": "npx prisma migrate deploy",
  "env": {
    "NODE_ENV": "production"
  }
}
```

Или создайте скрипт в `package.json`:

```json
{
  "scripts": {
    "postinstall": "prisma generate",
    "vercel-build": "prisma migrate deploy && next build"
  }
}
```

---

## 📋 Чек-лист настройки

- [ ] Зарегистрирован аккаунт на Neon/Supabase
- [ ] Создана база данных
- [ ] Скопирована строка `DATABASE_URL`
- [ ] Добавлена переменная `DATABASE_URL` в Vercel
- [ ] Выполнены миграции (`npx prisma migrate deploy`)
- [ ] Инициализирована база данных (`npm run init-db`)
- [ ] Проверена работа приложения

---

## 🔍 Проверка подключения

После настройки проверьте:

1. **Через API endpoint:**
   ```
   https://your-domain.vercel.app/api/health-check
   ```

2. **Через Vercel CLI:**
   ```bash
   vercel env pull .env.local
   npx prisma studio
   ```

---

## ⚠️ Важные замечания

1. **Бесплатные лимиты:**
   - Neon: 0.5 GB, подходит для большинства проектов
   - Supabase: 500 MB, достаточно для старта

2. **Резервное копирование:**
   - Neon и Supabase автоматически делают бэкапы
   - Для важных данных рекомендуется настроить дополнительные бэкапы

3. **Миграция данных:**
   - Если у вас уже есть локальная SQLite база, используйте скрипт `scripts/export-sqlite-data.js` для экспорта
   - Затем импортируйте данные в новую PostgreSQL базу

---

## 🆘 Проблемы и решения

**Ошибка: "Can't reach database server"**
- Проверьте правильность `DATABASE_URL`
- Убедитесь, что добавили `?sslmode=require` в конец строки для Neon

**Ошибка: "Database does not exist"**
- Проверьте название базы данных в `DATABASE_URL`
- Создайте базу данных заново

**Миграции не применяются:**
- Выполните `npx prisma migrate deploy` вручную
- Проверьте права доступа к базе данных


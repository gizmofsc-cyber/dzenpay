# 🚨 Исправление ошибки 500 на Vercel

## Проблема
При авторизации на https://testovoe-xi.vercel.app/login возникает ошибка 500.

## Причины
1. **Отсутствуют переменные окружения** на Vercel
2. **Не настроена база данных** PostgreSQL
3. **Не применены миграции** базы данных

## 🔧 Решение

### 1. Настройка переменных окружения в Vercel

Перейдите в панель управления Vercel → Settings → Environment Variables и добавьте:

```env
# Обязательные переменные
DATABASE_URL=postgresql://username:password@host:port/database
NEXTAUTH_SECRET=your-secret-key-minimum-32-characters
NEXTAUTH_URL=https://testovoe-xi.vercel.app
NODE_ENV=production
```

### 2. Создание PostgreSQL базы данных

#### Вариант A: Vercel Postgres (рекомендуется)
1. В панели Vercel → Storage → Create Database
2. Выберите PostgreSQL
3. Скопируйте `DATABASE_URL` из настроек базы
4. Добавьте в Environment Variables

#### Вариант B: Внешняя база данных
- **Neon**: https://neon.tech (бесплатно)
- **Supabase**: https://supabase.com (бесплатно)
- **Railway**: https://railway.app (бесплатно)

### 3. Применение миграций

После создания базы данных выполните миграции:

```bash
# Через Vercel CLI
vercel env pull .env.local
npx prisma migrate deploy
npx prisma generate
```

### 4. Инициализация данных

Создайте админа и базовые данные:

```bash
# Создание админа
npm run init-db

# Инициализация сетевых пар
npm run init-network-pairs
```

## 🔍 Диагностика

### Проверка состояния системы
Откройте: https://testovoe-xi.vercel.app/api/health-check

Этот endpoint покажет:
- Статус переменных окружения
- Подключение к базе данных
- Количество пользователей
- Данные для входа админа

### Проверка логов
В панели Vercel → Functions → View Function Logs

## 📋 Данные для входа

После настройки используйте:
- **Email**: `admin10@gmail.com`
- **Пароль**: `datmuf-Bajjyk-6wupde`

## 🚨 Частые ошибки

### 1. DATABASE_URL не настроен
```
Error: Can't reach database server
```
**Решение**: Добавьте `DATABASE_URL` в Environment Variables

### 2. Миграции не применены
```
Error: Table 'User' doesn't exist
```
**Решение**: Выполните `npx prisma migrate deploy`

### 3. NEXTAUTH_SECRET не настроен
```
Error: Missing NEXTAUTH_SECRET
```
**Решение**: Добавьте `NEXTAUTH_SECRET` (минимум 32 символа)

### 4. База данных пустая
```
Error: User not found
```
**Решение**: Выполните `npm run init-db`

## 📞 Поддержка

Если проблемы остаются:
1. Проверьте логи в Vercel Dashboard
2. Проверьте переменные окружения
3. Обратитесь к менеджеру: [@alltimez](https://t.me/alltimez)

---

**После выполнения всех шагов проект должен работать корректно! 🚀**
